#include "main.h"
#include "stdlib.h"
/**
 * main- Returns a pointer to the allocated memory
 *
 * Return: Always 0
 * /

void *malloc_checked(unsigned int b)

{
	void *ptr;
	ptr=malloc(b);
	if (s != NULL)
	{
		exit(98);
	}
	return (ptr);
}
